import React from 'react';

const AppointmentCard = () => {
  return (
    <div>
      {/* Card to display appointment details in a list */}
      <p>Appointment Card Component</p>
    </div>
  );
};

export default AppointmentCard;
