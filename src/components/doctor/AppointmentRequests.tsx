import React from 'react';

const AppointmentRequests = () => {
  return (
    <div>
      {/* List of pending appointment requests for the doctor to approve/reject */}
      <p>Appointment Requests Component</p>
    </div>
  );
};

export default AppointmentRequests;
